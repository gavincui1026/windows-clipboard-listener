# ===== NVIDIA GPU (CUDA 12.x，包括 12.8) =====
cupy-cuda12x==13.0.0          # CuPy 对应 CUDA 12.x 的预编译包

# Numba（CUDA JIT）
numba==0.60.0

# （可选）PyCUDA 低级接口
# pycuda==2024.1.2

# ===== 跨平台 GPU (OpenCL) =====
pyopencl==2024.2.7

# ===== 深度学习（可选）=====
# PyTorch（CUDA 12.8 预编译轮子）
# 安装命令（不要直接写到本文件里，以免 index-url 影响全局）：
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# JAX（CUDA12）——使用官方索引页安装（同样不写入文件）：
#   pip install -U "jax[cuda12]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html

# ===== 其他依赖 =====
numpy==1.26.4